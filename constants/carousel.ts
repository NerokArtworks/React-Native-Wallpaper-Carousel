import { Dimensions } from "react-native";

const { width } = Dimensions.get('screen');
export const _imageWidth = width * 0.7;
export const _imageHeight = _imageWidth * 1.76;
export const _spacing = 24;